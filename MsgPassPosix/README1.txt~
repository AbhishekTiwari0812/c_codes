==========
[1]Executing the files

There is a  bash.sh file included in the folder.
The format (arguments) for compiling and running are typed there.

The command line arguments are the temperatures of the central and the other four 
external processes respectively.
=========

==>>  IF THE CODE HALTS IN MIDDLE OF EXECUTION....!!!  <<==

		 compile and run clear_and_run.c file
		 It will clear out the previous processes and 
		 
===========


